# CodexProject(6) 검토 요약

## 전체 평가

-   CodexProject(5) 대비 구조가 더욱 안정화되었다.
-   신규 기능 추가보다는 기술 부채 제거와 구조 개선에 초점을 맞춘
    버전이다.
-   Source of Truth 통합, Loader 대칭성, 중복 제거 등 유지보수성이 크게
    향상되었다.

------------------------------------------------------------------------

# 주요 개선 사항

## 1. Regime 정책 단일화

-   `REGIME_SIDES`를 하나의 Source of Truth로 통합.
-   training.py와 cli.py가 동일 객체를 공유.
-   정책 불일치 가능성을 제거.

## 2. attach_labels 중복 제거

-   동일 Holdout 데이터에서 Long/Short가 같은 라벨을 두 번 계산하던 문제
    제거.
-   라벨 계산 후 target만 분리하여 사용.
-   학습 시간이 감소.

## 3. CentroidLinearClassifier 구현

-   기존에는 ImportError가 발생하는 죽은 코드였음.
-   실제 모델 구현 추가.
-   ModelAdapter 인터페이스(as_dict/from_dict 포함)를 모두 지원.

## 4. Loader Symmetry

-   저장 가능한 모델을 다시 정상적으로 Load 가능하도록 수정.
-   Save → Load → Predict 흐름이 완성됨.

## 5. MappingProxyType 적용

-   정책 객체를 읽기 전용으로 변경.
-   실수로 정책이 변경되는 문제 방지.

------------------------------------------------------------------------

# 아직 남은 주요 과제

## Live Kelly

-   아직 Live에서는 entry_quantity = 0.001 하드코딩.
-   Kelly PositionSizer는 정의되어 있지만 실제 주문에는 연결되지 않음.

## \_ctx_two_sided

-   현재 bool 플래그 방식.
-   향후 capability 기반 구조로 개선하면 유지보수성이 향상될 것으로
    판단.

------------------------------------------------------------------------

# 최종 의견

현재까지 검토한 버전 중 가장 구조가 안정적이다.

재학습 전에는 다음만 확인하면 된다.

1.  Live Kelly 배선 여부
2.  default_fallback = 0
3.  shorts_skipped_no_short_model = 0
4.  모든 regime/side 모델 생성 확인

------------------------------------------------------------------------

# CentroidLinearClassifier란?

Centroid(센트로이드)는 각 클래스의 평균 위치(무게중심)를 의미한다.

학습 과정 1. 모든 Long 샘플의 평균 벡터 계산 2. 모든 Short 샘플의 평균
벡터 계산 3. 두 평균점의 차이를 이용해 하나의 선형 결정경계를 생성

예시

Long 평균 (10, 5)

Short 평균 (2, 1)

새로운 데이터 (8, 4)

→ 어느 평균점에 더 가까운지를 계산하여 Long 또는 Short를 예측한다.

특징 - 매우 빠른 학습 (거의 즉시) - 추론 속도 매우 빠름 - 메모리
사용량이 적음 - 복잡한 비선형 패턴은 잘 학습하지 못함

현재 프로젝트에서는 주력 모델이 아니라 백테스트, 스모크 테스트, 비교
실험용의 매우 가벼운 기준(Baseline) 모델 역할을 한다.

CatBoost나 Multi-Horizon 모델을 대체하려는 목적은 아니다.
