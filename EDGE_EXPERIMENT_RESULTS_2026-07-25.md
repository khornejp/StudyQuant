# 엣지 실험 결과 — 배리어/호라이즌 정렬 (2026-07-25)

기준 커밋: `24d4c7a` 이후. 데이터 2020~2024 학습 / 2025 OOS.

---

## 0. 한 줄 결론

> **평균회귀 신호는 실재하나(gross 엣지 t=3.55, 통계 유의) 거래비용 미만이다.**
> 신호 부재가 아니라 **엣지-비용 비율** 문제. feature/model이 아니라 실행비용이 병목.

---

## 1. 동기 — IC가 가리킨 신호-라벨 불일치

`ic_report.csv` 실측 (fold-stable, 누수 플래그 0):

| horizon | \|IC\|≥0.05 | max\|IC\| | 상위10 부호 |
|---|---:|---:|---|
| **15m** | **22** | 0.0595 | 10 음 |
| 30m | 4 | 0.0539 | 10 음 |
| **60m (기존 배리어)** | **0** | 0.0476 | 9 음 |
| 120m / 240m | 0 | ~0.044 | 9 음 |

상위 피처(close_ema_26_ratio, rsi_14, close_zscore_60, ema_12_26_spread = 이평 대비 과매수) 전부 **음의 IC = 평균회귀**. 신호는 **15m에서 최강, 60m엔 소멸.** 그런데 배포 배리어는 60바 = 신호가 죽은 지점.

가설: 배리어/호라이즌을 15m 신호 스케일에 맞추면 예측력이 살아난다.

---

## 2. 실험 — unified 15m / 0.4%·0.2%

`train`(non-regime, long-only, untuned) `--horizon 15 --tp-pct 0.004 --sl-pct 0.002`.

### 2.1 예측력 (60m 대비)

| | F1 | Brier | Acc | ECE |
|---|---:|---:|---:|---:|
| 60m / 1%·0.5% (기존) | 0.018 | 0.223 | 0.662 | 0.042 |
| **15m / 0.4%·0.2%** | **0.139** | **0.174** | 0.748 | 0.036 |

F1 **7.8배**, Brier 하락. 60m에선 모델이 양성을 거의 못 맞췄는데 15m엔 신호 존재. IC 가설 확증.

### 2.2 거래 — 세 번의 백테스트로 원인 규명

| 설정 | 거래 | gross | net | 승률 | 비고 |
|---|---:|---:|---:|---:|---|
| Kelly, thr=strategy 0.5 | 0 | — | — | — | 전 봉 SELL → long-only라 스킵 |
| Kelly, thr=0.34, long만 | 13 | +0.04% | −0.06% | 46.15% | Kelly가 p>0.467만 통과 → 13봉 |
| **고정, thr=0.34, long만** | **3,096** | **+4.63%** | **−18.3%** | 37.9% | 전 진입모집단 |

- 1행: 단일모델 백테스트가 **학습 임계값(0.34)을 안 쓰고 전략 기본 0.5**를 사용 → 확률 전부 <0.5 → 전 봉 SELL → long-only라 short 모델 없어 0거래.
- 3행(핵심): **gross/거래 +1.465 bps, t-stat +3.55 (유의).** 근데 왕복비용 **8 bps**(2×(fee 2+slip 2)) → **net/거래 −6.5 bps**. 비용이 엣지를 5.5배 압도.

---

## 3. 판정

**Verdict: PROMISING BUT UNPROVEN → 사실상 REJECT (현 비용 조건)**

- **신호 실재**: 15m gross 엣지 통계 유의(t=3.55). 60m(gross 음수)과 질적으로 다름. 랭킹 정상(고확률→고승률: p>0.467인 봉 승률 46%로 breakeven 근접).
- **비용 미만**: 엣지 1.5 bps ≪ 왕복비용 8 bps. maker(fee 0)라도 4 bps > 1.5 bps. 비용 0이어도 gross_sharpe 0.06으로 매우 약함.
- **결론**: 이 feature/비용 조건에서 15m long 평균회귀는 tradeable 아님. 신호가 비용 장벽 아래에 있음.

원래 질문("왜 엣지 없나")의 답: **신호 부재가 아니라 신호가 비용보다 작음.**

---

## 4. 미검증 / 다음 실험

1. **short-side** — 평균회귀의 강한 쪽은 과매수→하락=SHORT. long이 breakeven 근처였으니 short gross 엣지가 더 클 가능성. (short_success 15m 모델 필요 → `train --target` 노브 추가.)
2. **30m sweet-spot** — 15m은 엣지 작고 60m은 신호 소멸. 30m이 거래당 이동 크면서(비용 비중↓) 신호 유지하는 중간점인지.

두 실험 모두 재학습+백테스트(각 ~3시간). 결과는 본 문서에 추기.

---

## 5. 부수 발견 (버그/갭)

- **단일모델 백테스트가 모델의 학습 임계값을 무시**하고 전략 기본 0.5를 씀(`effective_thresholds={}`). regime 번들만 `selected_thresholds`를 적용. → 단일모델도 `threshold_report.json`을 읽게 배선 필요(문서 §17 연결).
- unified 경로 long-only(profitability 타겟) → 평균회귀 short 신호 실행 불가. `--target` 또는 3클래스 타겟 필요.
